# The backend is now a stateless tool API (PDF editor, converter, DockerHub
# search, country indices) — no database, no auth. It only needs Redis, and
# defaults to the "redis" service host, so this file is optional.
REDIS_HOST=redis
REDIS_PORT=6379
